# Release Notes 1.2.8

- Fix: حفظ إعداد `purchase_price_type` في قاعدة البيانات (MySQL) عبر `save_settings.php` بدل الاعتماد على LocalStorage فقط.
- Fix: في الاستلام والمرتجع، التحقق من أن سعر التكلفة/سعر المصنعية أكبر من 0 قبل السماح بالحفظ.
- Fix: صفحة `تأكيد الاوردرات` أصبحت معزولة التأثير:
  - `تأكيد` و`تأجيل` يؤثران على صفحة التأكيد فقط (تكليفات الصفحة).
  - `إلغاء` فقط هو الذي يغيّر حالة الأوردر إلى `ملغي`.
  - إزالة أي تأثيرات جانبية إضافية من هذا المسار.
- Fix: تصفير يومي تلقائي لتكليفات صفحة تأكيد الأوردرات (إزالة تكليفات الأيام السابقة تلقائياً).
- Fix: صفحة `بدء اليومية` تقبل الآن الحالات: `قيد الانتظار` + `مرتجع` + `مؤجل` + `ملغي`.
- UI: تكبير خانة فلترة الحالة في صفحة إدارة الأوردرات لتحسين الاستخدام.
