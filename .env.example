# Copy to .env.local for local development
# Never commit real keys (.env* is gitignored)

# Optional: Gemini API key (if used)
GEMINI_API_KEY=PLACEHOLDER_API_KEY

# Optional: when running Vite dev server, tell proxy where Apache serves the project
# Examples:
# VITE_PHP_BASE_PATH=/Nexus
# VITE_PHP_BASE_PATH=/clients/Nexus
VITE_PHP_BASE_PATH=
