# Changelog

## 1.10.17 (2026-09-23)
- Feature: في صفحة إغلاق اليومية (`SalesDailyClose`)، إظهار كافة أوردرات المندوب (بما فيها أوردرات النزول والمؤجلة) تلقائياً في قائمة «العهدة الحالية مع المندوب»، ليتسنى للمستخدم تحديد ما تم تسليمه وما تم تأجيله يدوياً بكل سهولة.
- Feature: إضافة زر «إرجاع الكل» في صندوق «المؤجل لليومية» لإرجاع كافة الأوردرات المؤجلة فوراً وبضغطة واحدة إلى العهدة الحالية.
- Fix: تحديث كاش الـ Service Worker إلى `dragonpro-cache-v32` ليتم تطبيق التحديث وتحديث الواجهة على كافة أجهزة وسيرفرات المستخدمين تلقائياً.

## 1.10.16 (2026-09-23)
- Fix: إصلاح شامل لصفحة إغلاق اليومية (`SalesDailyClose`) لإظهار كافة الأوردرات الفعلية الموجودة في عهدة المندوب بالكامل، بما يشمل أوردرات النزول المؤجلة السابقة (`postponed` / `deferred`) وأوردرات اليومية الجديدة (`with_rep`)، لتتطابق تماماً مع ما يظهر في شاشة تسجيل المرتجعات.
- Fix: إدراج أوردرات العهدة النشطة للمندوب وربطها باليومية المفتوحة تلقائياً في استعلام الخادم `getJournalOrders` مع جلب بيانات وتفاصيل المنتجات كاملة.
- Fix: تصنيف الأوردرات المؤجلة في قائمة "المؤجل لليومية (نزول)" مع تمكين إرجاعها إلى العهدة النشطة أو تسليمها أو إثبات نزولها، وإظهار إجمالي القطع والمبالغ بدقة متناهية.

## 1.10.15 (2026-09-20)
- Fix: إصلاح وعزل صفحة إغلاق اليومية للمناديب (`SalesDailyClose`) بالكامل ومنع تسريب أي أوردرات تاريخية قديمة أو معلقة من خارج اليومية المفتوحة، وإلغاء جلب العهدة العامة وحذف دمج `extraDeferred`.
- Fix: تصحيح الحسابات الرياضية للقطع المسلمة والقيمة المسلمة في حالات الارتجاع الجزئي عبر خصم المرتجع بدقة ومنع التضخيم أو التكرار المحاسبي.
- Fix: عزل صارم بين إحصائيات المستلم والمرتجع والنزول (المؤجل) لضمان دقة التقارير وطباعة اليومية.
- Fix: تصحيح لوجو الفوتر في النظام وربطه مباشرة بشعار دراجون برو `Dragon.png` بنمط `object-contain` بدلاً من لوجو المتجر المخزن في المتصفح.
- Fix: ضبط خط أنابيب البناء (`Vite Build Pipeline`) عبر استعادة نقطة الدخول البرمجية الكاملة `/index.tsx` في `index.html` وتحديث كاش الـ Service Worker إلى `v30`.
- Feature: دعم نسخ اسكربت الأوردرات من الواتساب مع ترويسات التواريخ والأوقات وأسماء المرسلين وتنقيتها تلقائياً في صفحة استيراد الأوردرات.
- Feature: تحديث تكلفة شراء المنتج تلقائياً بقيمة المصنعية المدخلة أثناء استلام المنتجات من المورد لتحسين واحتساب دقيق للأرباح.
- Fix: مراجعة وتدقيق شامل لمعادلات لوحة التحكم (Dashboard) والمبيعات وصافي الأرباح.

## 1.10.7 (2026-09-13)
- Fix: إصلاح عزل استعلام أوردرات اليوميات المفتوحة ومنع تسريب الأوردرات المرتجعة التاريخية القديمة إلى اليومية الجديدة للمناديب، وإلغاء الدمج الاحتياطي التلقائي للأوردرات المرتجعة من جدول `orders` عند الاستعلام عن يومية محددة (`getJournalOrders`).
- Fix: فلترة أوردرات المرتجع والتسليم في واجهة إغلاق اليومية (`SalesDailyClose`) بمطابقة رقم اليومية المفتوحة (`openJournalId`) حصرياً لمنع ظهور أي أوردر لا يخص اليومية الحالية نهائياً.

## 1.10.6 (2026-09-13)
- Fix: إصلاح عدم ظهور أوردرات النزول المؤجلة في صفحة بدء اليومية للمناديب (`SalesDaily`) وتضمين حالة `postponed` في استعلام العهدة النشطة `getByRep` والأوردرات الافتتاحية في `startDaily`.
- Fix: تصفير إحصائيات وأوردرات المندوب بالكامل في صفحة إغلاق اليومية (`SalesDailyClose`) عند عدم وجود يومية مفتوحة للمندوب، وإظهار تنبيه يوضح ضرورة بدء اليومية أولاً، ومنع استرجاع الأرشيف التاريخي القديم.
- Fix: منع تسريب الأوردرات المرتجعة السابقة إلى اليوميات الجديدة للمندوب، وضمان عدم ربط المرتجع باليومية إلا إذا كان ينتمي لتاريخها، واستبعاد المرتجعات القديمة من قائمة الأوردرات المتاحة عند بدء اليومية.
- Fix: مطابقة دقيقة وصارمة لباركود بوالص الشحن والأصناف وحل مشكلة ظهور أوردر برقم مختلف عند مسح الباركود عبر معالجة الفروق بين أرقام الأوردرات المطبوعة والمعرّفات الداخلية وتجريد رموز التحكم المخفية.

## 1.10.5 (2026-09-12)
- Fix: إصلاح خطأ قيد الفهرس الفريد `Duplicate entry 'uk_rep_order'` في جدول `rep_journal_orders` الذي كان يتسبب في فشل جلب أوردرات اليومية وظهور أعداد الأوردرات المرتجعة والمسلمة والنزول كأصفار في شاشة إغلاق اليومية للمناديب.
- Fix: إسقاط الفهرس القديم `uk_rep_order` تلقائياً واستبداله بالفهرس المركب `(rep_id, order_id, journal_id)` وتأمين حلقة مزامنة الأوردرات لمنع أي توقف في واجهة إغلاق اليومية.

## 1.10.4 (2026-09-12)
- Fix: حل مشكلة ظهور العنوان القديم في بوليصة الشحن عند طلب أوردر جديد لنفس العميل، وفصل وحفظ عنوان ومحافظة التوصيل بشكل مستقل لكل أوردر في `orders.address` و `orders.governorate`.
- Feature: إلغاء قفل وتجميد حقول العميل في شاشة إضافة الأوردر اليدوي عند اختيار عميل حالي، وإتاحة تعديل العنوان والمحافظة والهاتف بحرية كاملة لكل أوردر.
- Feature: الكشف والملء التلقائي الذكي لبيانات العميل بمجرد إدخال رقم هاتفه في شاشة الأوردر اليدوي مع إبقاء الحقول قابلة للتعديل.
- Enhancement: دعم كامل لاستيراد الأوردرات من الإكسيل والاسكربت وتطبيق العنوان والمحافظة الجديدة للأوردرات المستوردة في قاعدة البيانات والبوليصة مباشرة.
- Feature: حماية الأوردر وقفل تعديل بياناته وأصنافه برمجياً أثناء وجوده في عهدة المندوب لمنع الأخطاء المحاسبية والتلاعب أثناء التوزيع.
- Fix: تسوية اليومية المالية ومنع تكرار القيود المحاسبية عند تنفيذ الإرجاع الجزئي أو الإرجاع لمخزن المندوب.
- Enhancement: إضافة دعم `action=get` لاسترجاع بيانات الأوردر الفردي بدقة في واجهة طباعة البوالص `PrintWaybill`.

## 1.10.3 (2026-09-12)
- Feature: إضافة قوائم منسدلة (`<select>`) ديناميكية مخصصة تحت حقول اللون والمقاس في بطاقات استيراد الأوردرات لاختيار الألوان والمقاسات المسجلة فعلياً في قاعدة البيانات بنقرة واحدة عند عدم تطابق المدخلات.
- Feature: إضافة لوحة تقرير فحص وملاحظات نص الأوردرات أعلى قسم المعاينة تُبرز الأوردرات التي تحتوي على مشاكل (منتج غير مسجل، لون غير مسجل، مقاس غير مسجل، عدم تطابق الإجماليات) مع عرض الألوان والمقاسات المتاحة لكل منتج.
- Feature: عرض سطر النص الأصلي المستخرج من نص الرسالة في بطاقة كل منتج للمقارنة السريعة والتأكد من مطابقة الطلب.
- Feature: التعيين الذكي التلقائي للمتغيرات (لون أو مقاس) عندما يمتلك المنتج متغيراً واحداً مسجلاً فقط ولم يُذكر في نص الأوردر لتسريع العملية دون إظهار تنبيهات غير ضرورية.
- Enhancement: تعزيز محرك تطبيع الحروف العربية المتقدم (Normalizing Arabic Text) لدعم التاء المربوطة والهاء والهمزات والألف والياء والمسافات لكافة أسماء المنتجات والألوان والمقاسات (مثل: سلوبته/سلوبتة، اسود/أسود، كحلي/كحلى).
- Enhancement: دعم مرونة ترتيب البيانات في أسطر تفاصيل المنتجات بالاسكربت (المقاس قبل اللون أو العكس، السعر، الكمية) وحفظ السعر المستخرج بدقة.
- Enhancement: تحديثات وتطويرات موسعة على مصمم البوالص المتقدم (`UniversalWaybillRenderer` و `WaybillBuilderAdvanced`) وتحسين إمكانيات تحرير وتخصيص البوالص والطباعة الحرارية.

## 1.9.9 (2026-09-07)
- Fix: ضبط وتوحيد مقاس طباعة "إذن تسليم بضاعة" في صفحة بدء اليومية وصفحة إدارة المناديب وصفحة تحديث الحالات ليتطابق بدقة مع مقاس الورق الحراري 80MM (70mm عرض فعلي) ومنع خروج المحتوى أو قص الحواف.
- Fix: تقييد وتصغير باركود إذن التسليم SVG (`max-width: 100%`) لمنع تمدد الصفحة خارج حدود الورقة الحرارية وضمان سرعة قراءته بالماسح الضوئي.
- Feature: دمج معرض قوالب بوالص الشحن الـ 50 كاملة داخل صفحة "مصمم البوليصة" كعلامة تبويب مخصصة مع إمكانية التعيين كافتراضي أو التعديل المباشر.
- Feature: فصل قوالب النظام الجاهزة عن قوالب المستخدم المخصصة في مصمم البوالص لتنظيم أفضل وسهولة الوصول.
- Fix: إصلاح دقة تحميل وتعديل القوالب المختارة (مثل قالب 21) في المحرر الاحترافي بمطابقة بصرية وهيكلية 100% لكافة العناصر واللوجو والبيانات.
- Fix: معالجة تباعد العناصر السفلية في المحرر الاحترافي لمنع التصاقها بجدول المنتجات.
- Fix: تفعيل المعاينة الحية لقوالب المستخدم المخصصة في الكانفاس والطباعة.
- Refactor: إزالة صفحة قوالب بوالص الشحن المنفصلة من القائمة الجانبية ومن تبويبات الإدارة والنظام والاعتماد على المعرض المدمج بمصمم البوليصة.

## 1.9.8 (2026-09-06)
- Feature: جعل جميع القوائم المنسدلة (Dropdown Lists) في النظام قابلة للبحث المباشر والتصفية الذكية مع تطبيع الحروف العربية (إلغاء التشكيل والهمزات).
- Feature: إتاحة خانة البحث في كافة القوائم المنسدلة بغض النظر عن عدد العناصر بها.
- Feature: إضافة حقول البيانات الديناميكية الجديدة في مصمم البوليصة المتقدم: "التاريخ" و"سياسة الشركة / الشروط".
- Feature: إضافة زر "تعديل في المصمم المتقدم" في صفحة قوالب بوالص الشحن الـ 50 لتحويل أي قالب مجهز فوراً إلى عناصر تفاعلية قابلة للسحب والإفلات والتعديل في المصمم المتقدم.
- Fix: معالجة واستقرار قفل المعاملات (InnoDB Deadlocks Error 1213) في `components/api.php` عبر الترتيب الموحد وإعادة المحاولة التلقائية حتى 5 مرات.
- Fix: معالجة وإخلاء Buffer الاستجابات لمنع أخطاء JSON Parse Error في صفحة إتمام اليومية والإرجاع للمخزن.


## 1.9.6 (2026-09-04)
- Fix: إصلاح خطأ 502 عند التثبيت الأول بسبب فشل الاتصال بسيرفر التفعيل - النظام يكمل التثبيت الآن بوضع Trial تلقائياً ويمكن التفعيل لاحقاً من الإعدادات.
- Fix: إصلاح ملف `manifest.json` الذي كان يُحفظ بترميز خاطئ مما يسبب `Syntax error` في المتصفح.
- Fix: زيادة حدود `memory_limit` و `max_execution_time` في `setup.php` لتفادي timeout عند استيراد قاعدة البيانات الكبيرة.
- Fix: `settings.php` وباقي الـ components لا تعمل بعد التثبيت بسبب غياب `config.php` (كان يحدث بسبب 502 في التفعيل).

## 1.9.5 (2026-09-04)
- Fix: إصلاح ملف قاعدة البيانات المُستخدم في التثبيت الأول (`current_schema_struct.sql`) - كان ناقصاً 14 جدول و14 عمود.
- Fix: تحديث ملف schema التثبيت ليشمل جميع الجداول الـ 88 الموجودة في قاعدة البيانات الحية.
- Fix: إضافة الجداول الناقصة: `app_settings`, `app_files`, `app_feedback`, `delivery_notes`, `delivery_note_lines`, `order_confirmation_assignments`, `product_variants`, `rep_close_events`, `rep_daily_journal`, `rep_delivery_sessions`, `rep_journal_orders`, `rep_return_events`, `selected_products`, `user_preferences`.
- Fix: إضافة الأعمدة الناقصة في جدول `orders` (discount, tax, employee, page, items_json) وجدول `products` (is_archived, created_at) وجدول `transactions` (category, created_by) وجدول `treasuries` (type).
- Update: أي مستخدم يثبّت DragonPro لأول مرة يحصل الآن على قاعدة بيانات كاملة ومحدثة بدون الحاجة لتشغيل migrations يدوياً.
- Tool: إضافة أدوات `Database/generate_schema_struct.php` و `Database/compare_columns.php` لمقارنة وتوليد schema التثبيت تلقائياً من قاعدة البيانات الحية.

## 1.9.4 (2026-09-02)
- Fix: إصلاح ظهور شعار الشركة (اللوجو) في جميع مناطق النظام (شريط التنقل، صفحة الإعدادات، بوالص الشحن، الفواتير).
- Fix: الشعار يُحفظ الآن كـ Base64 Data URI مباشرةً بدلاً من مسار نسبي، مما يضمن ظهوره بغض النظر عن المنفذ أو الجهاز.
- Fix: الشعار يظهر فوراً في الشريط العلوي بعد الرفع دون الحاجة لإعادة تحميل الصفحة (live update عبر localStorage + storage events).
- Fix: إضافة fallback تلقائي في حالة فشل تحميل الصورة يعيدها للشعار الافتراضي Dragon.png.
- Fix: `get_settings.php` و `verify.php` يُرجعان الشعار دائماً كـ data URI سواء كان مخزناً مباشرة أو كمسار uploads/.
- Fix: حفظ الشعار تلقائياً في localStorage عند تحميل صفحة الإعدادات لضمان ظهوره في جميع المكونات.

## 1.9.0 (2026-08-29)
- Feature: إضافة محرك قوالب بوالص الشحن الشامل (`UniversalWaybillRenderer`) مع 21 قالباً وتصميماً متميزاً كلياً يلبي مختلف الأنشطة التجارية (الملابس، البوتيك، الطرود، الشحن السريع، الفواتير الضريبية، والإيصالات المصغرة).
- Feature: إضافة صفحة إدارة ومعاينة قوالب بوالص الشحن (`WaybillTemplatesManager`) مع محاكاة واقعية واختيار النموذج الافتراضي بضغطة زر.
- Feature: إضافة نموذج بوليصة البوتيك والملابس الراقية الجديد (`Template21_SuitsBoutique`) بريبون أنيق وسطر عنوان مستقل وبيان مقاسات وألوان متكامل.
- Feature: توحيد مقاس جميع القوالب لتعمل بدقة بمقاس ربع ورقة A4 (Quarter-A4) مع تقسيم وتجميع 4 بوالص في كل ورقة A4 كاملة (2x2 Grid).
- Update: توحيد واكتمال كافة البيانات الإلزامية في جميع القوالب الـ 21 (الباركود، رقم الأوردر، اسم العميل، الهاتف، المحافظة، العنوان، جدول المنتجات، مصاريف الشحن، المطلوب تحصيله، الملاحظات، الموظف، البيدج، وسياسة الشركة).
- Feature: إضافة نظام تدقيق ومطابقة أرصدة المناديب (`auditAndReconcileReps`) في صفحة إدارة المناديب لفحص المديونيات ومطابقتها آلياً مع الأوردرات المسلمة والتوريدات وحركات الخزينة والتسويات اليدوية.
- Fix: إصلاح ظهور قائمة قوالب بوالص الشحن في القائمة الجانبية وإزالة التكرارات.

## 1.6.1 (2026-07-24)
- Fix: تسجيل اسم الموظف الذي قام بعملية التحويل بين الخزائن (`created_by`) لضمان ظهوره بسجل المعاملات والطباعة.
- Fix: إصلاح تقرير "بضائع عهدة المندوب" بربطه بنقطة النهاية الصحيحة لقائمة المناديب (`users&action=getAllWithBalance&related_to_type=rep`).
- Feature: إعادة هيكلة وتفصيل عرض المنتجات بتقرير عهدة المندوب ليشمل (اسم المنتج/الصنف، اللون، المقاس، الباركود، والمناديب المسند لهم).
- Fix: إضافة ميزة التحليل والاستخراج الذكي لبيانات اللون والمقاس المدمجة بداخل نصوص أسماء المنتجات لتنفصل وتظهر بشكل منظم في جدول التقرير وطباعته وتصديره.
- Fix: معالجة تسجيل المرتجعات عبر الباركود في صفحة `SalesUpdateStatus` بفرز وإزالة الأوردرات المرتجعة فوراً من القائمة المحلية لمنع تكرارها أو بقائها، ومنع الخصم المزدوج للمبالغ من عهدة المندوب.
- Fix: السماح بحذف الخزينة بصفحة إدارة الخزائن (`FinanceModule`) عند تقييم رصيدها بالقيم العددية (`0` أو `"0.00"`).
- Feature: إضافة حالة "مؤجل" (`postponed`) في صفحة تأكيد الأوردرات مع تبويب مخصص وإمكانية التطبيق السريع أو عبر الباركود.
- Feature: دعم وإمكانية قبول الأوردرات ذات الحالة "مؤجل" لإضافتها في صفحة بدء اليومية (`SalesDaily`) سواء بالاختيار أو عبر مسح الباركود.
- Fix: ترجمة مسميات الحالات `no_answer` إلى "لا يرد" و `closed` إلى "مغلق" و `confirmed` إلى "مؤكد" بالكامل للغة العربية في صفحة تأكيد الأوردرات وبدء اليومية.

## 1.4.4 (2026-04-26)
- Feature: مزامنة الإعدادات سحابياً بالكامل عبر الأجهزة، وضمان تحميل كافة التفضيلات من السيرفر فور تسجيل الدخول.
- Fix: إزالة أزرار الحذف (سحب أوردر) من قسم "أوردرات النزول" في صفحة اليومية لتعزيز الأمان ومنع التلاعب في عهدة المندوب.
- Fix: استقرار أسعار المنتجات عند تعديل الأوردرات؛ تم ضبط النظام لاحترام السعر الأصلي للأوردر وعدم تغييره تلقائياً من قائمة المنتجات إلا بطلب المستخدم.
- Fix: تصحيح إحصائيات تقارير المبيعات؛ تقسيم الأوردرات ذات "المرتجع الجزئي" بدقة بين إجمالي المبيعات والمرتجع لضمان أرقام واقعية.
- Update: توحيد مفاتيح الإعدادات (Settings Keys) في النظام بالكامل لضمان التوافق بين السيرفر والواجهة والمتصفح.


## 1.3.4 (2026-03-17)
- Fix: ضبط عرض مديونية المورد في صفحة الاستلام/المرتجعات ليعكس مفهوم الدين بشكل صحيح (حتى مع الأرصدة السالبة).
- Fix: صفحة إنشاء الأوردر اليدوي أصبحت تعرض المنتج مرة واحدة فقط مع تسلسل اختيار صحيح: المنتج ← المقاس ← اللون.
- Update: إعادة ترتيب نموذج سداد المورد في صفحة الإيراد/المصروف وإضافة حقول قراءة فقط للحساب الحالي والمتبقي بعد السداد.
- Fix: تصحيح معادلة "المبلغ المتبقي للمورد" في صفحة الإيرادات والمصروفات عند الرصيد السالب، بحيث يقل المتبقي باتجاه الصفر بعد السداد بدل زيادة السالب.
- Fix: توحيد اتجاه احتساب "سداد المورد" بين الماليات وصفحة الموردين وكشف الحساب اعتمادًا على الإشارة الفعلية للمبلغ، لمنع زيادة الرصيد بالخطأ بعد السداد.
- Fix: توحيد قاعدة السداد لتعمل في الحالتين (الرصيد الموجب/السالب) بحيث أي سداد مورد يحرك الرصيد دائمًا باتجاه الصفر في صفحة الموردين وكشف الحساب.
- Hotfix: استقرار احتساب رصيد المورد وكشف الحساب بمنطق ثابت غير متغير عبر الزمن (deterministic mapping) لمنع تغيّر إجماليات المورد بالكامل على بعض قواعد بيانات العملاء.
- Hotfix: إضافة `supplier_balance_before` داخل تفاصيل حركة سداد المورد وقت الحفظ، واستخدامه لتحديد اتجاه أثر السداد بدقة (نحو الصفر) في قائمة الموردين وكشف الحساب.
- Hotfix: تنفيذ تسوية تلقائية لمرة واحدة بعد التحديث لإعادة ضبط `total_credit/total_debit` لكل الموردين من دفتر الحركات، لضمان تطابق صفحة الموردين مع كشف الحساب على قواعد البيانات القديمة.
- Hotfix: توحيد عرض أعمدة كشف المورد في الواجهة بحيث `فاتورة شراء` تظهر دائمًا في الدائن (له) و`مرتجع/سداد` في المدين (لنا)، مع توحيد وصف الرصيد النهائي ليتطابق مع صفحة الموردين (`له/عليه/متزن`).
- Fix: تحسين عرض معاملات الماليات بعد الحفظ (تصفية سجل العمليات المالية + إظهار فوري للمعاملة الجديدة عند الحاجة).
- Fix: إصلاح كشف حساب المورد ليتعامل مع التاريخ المحلي بشكل صحيح بدل UTC لتفادي اختفاء حركات اليوم.
- Fix: جعل API كشف حساب المورد متوافقًا مع قواعد البيانات التي لا تحتوي على العمود `transactions.created_by` لمنع Fatal error.

## 1.3.3 (2026-03-16)
- Update: توحيد الإصدار الحالي للمشروع على `1.3.3` وتحديث إعدادات التحديث الذاتي لاستخدام حزمة `DragonPro_v1.3.3.zip`.
- Fix: صفحة إنشاء الأوردر اليدوي تعرض المنتجات بدون تكرار، مع تسلسل اختيار صحيح: المنتج ← المقاس ← اللون.

## 1.3.2 (2026-03-16)
- Fix: توحيد ومعالجة حفظ المرتجع من صفحة `تسجيل المرتجعات` بحيث يستخدم `rep_id` الصحيح الخاص بالأوردر نفسه لتفادي فشل الحفظ.
- Fix: توحيد حسابات المرتجع في `إغلاق اليومية` بين الكروت والتفاصيل مع دعم أدق للـ `partial_return` و`full_return`.
- Update: عرض **عدد القطع المرتجعة** داخل كارت `إجمالي مرتجع`.
- Update: احتساب وعرض قيم `إجمالي التسليم` و`المؤجل` بصافي القيمة **بدون مصاريف الشحن** في الكروت والتفاصيل.
- Update: تحسين تتبع بيانات المرتجع في backend عبر `rep_journal_orders` وحقول `returned_pieces/returned_value` مع fallback آمن للحالات التاريخية.

## 1.3.1 (2026-03-14)
- Feature: صفحة الاستلام تعرض الآن حقل "سعر التكلفة الجديد" بشكل واضح مع عرض "متوسط التكلفة المتوقع" قبل الحفظ.
- Fix: عند حفظ إذن الاستلام يتم تحديث `cost_price` بمتوسط تكلفة مرجّح (Weighted Average) للمنتجات والخامات والإكسسوارات.
- Feature: صفحة تأكيد الأوردرات تدعم حذف الأوردر من "الأوردرات الحالية" واسترجاع الأوردر من "الأوردرات الملغية" مع إعادة الحالة السابقة.
- Fix: دورة اليومية أصبحت أدق — لا إغلاق بدون يومية مفتوحة، والسماح بإغلاق بمبلغ 0، وإغلاق جميع اليوميات المفتوحة للمندوب لتفادي بقاء يومية قديمة.

## 1.3.0 (2026-03-13)
- Release: إصدار تجميعي شامل يتضمن جميع إصلاحات وتحسينات الإصدارات السابقة حتى 1.2.9 في حزمة مستقرة واحدة.
- Fix: حفظ إعداد purchase_price_type في قاعدة البيانات مع استمرار عمل التحقق من السعر في الاستلام والمرتجع حسب نوع السعر المختار.
- Fix: عزل قرارات صفحة تأكيد الأوردرات بحيث لا تؤثر على حالة الأوردر إلا في حالة الإلغاء، مع دعم تصفير يومي لتكليفات التأكيد.
- Fix: تحسين دورة اليومية بإضافة الحالات المطلوبة (pending, returned, postponed, cancelled) بما يضمن توافق التشغيل اليومي.
- Fix: تحسين نظام التحديثات لالتقاط ملف ZIP بالاسم المحدد أولاً ثم fallback لأي ZIP داخل الإصدار لتحسين توافق الإصدارات القديمة.
- Update: استعادة عرض جميع الإصدارات في نافذة التحديثات مع إبقاء حالة توفر ZIP واضحة لكل إصدار.
- Update: إبقاء شريط العنوان على الرابط الأساسي بدون hash أثناء التنقل داخل التطبيق.
- Feature: إضافة أدوات دعم التحديث للعملاء تشمل التثبيت التلقائي المتسلسل للتحديثات وصفحة مساعدة لتفعيل php_zip.

## 1.2.9 (2026-03-13)
- Fix: نظام فحص التحديثات أصبح يطابق ملف ZIP المراد أولاً ثم يعمل fallback تلقائي لأي ملف ZIP داخل نفس الإصدار، مما يحسن توافق الإصدارات القديمة ذات أسماء ملفات مختلفة.
- Fix: تمت استعادة عرض جميع الإصدارات في نافذة التحديثات كما كان سابقاً مع استمرار إظهار حالة "لا يوجد ملف ZIP" فقط عند عدم وجود ملف فعلي.
- Fix: إزالة hash من عنوان المتصفح أثناء التنقل داخل التطبيق، ليبقى الرابط على `http://localhost:3000/` دون `#/...`.
- Update: تجهيز الإصدار `v1.2.9` للنشر مع تحديث ملفات النسخة والتحديث التلقائي.

## 1.2.8 (2026-03-13)
- Fix: حفظ إعداد "سعر الشراء في الاستلام والمرتجع" (purchase_price_type) في قاعدة البيانات — كان يُحفظ في localStorage فقط ولا يُستعاد بعد مسح المتصفح.
- Fix: التحقق من أن سعر التكلفة / سعر المصنعية أكبر من صفر في إذن الاستلام وإذن المرتجع يعمل الآن بشكل صحيح بعد حفظ الإعداد في DB.
- Fix: صفحة تأكيد الأوردرات — أصبحت القرارات (تأكيد / تأجيل) لا تُغيّر حالة الأوردر؛ فقط الإلغاء يُغيّر الحالة إلى "ملغي".
- Fix: صفحة تأكيد الأوردرات — تُمسح التكليفات القديمة تلقائياً يومياً (تصفير يومي) وتظهر فقط تكليفات اليوم الحالي.
- Fix: صفحة بدء اليومية — تقبل الآن الأوردرات بحالة "مؤجل" إلى جانب "قيد الانتظار" و"مرتجع".
- Fix: صفحة إدارة الأوردرات — تكبير خانة تصفية الحالة مع القائمة المنسدلة.

## 1.2.7 (2026-03-08)
- Feature: إضافة إعداد "سعر الشراء" في الإعدادات لتحديد طريقة تسعير الشراء في إذن الاستلام وإذن المرتجع.
	- خيار "سعر التكلفة الكامل" → يظهر حقل تكلفة المخزن فقط.
	- خيار "سعر المصنعية" → يظهر حقل سعر المورد/المصنع فقط.
	- ينطبق الخيار على صفحة الاستلام وصفحة المرتجع وحسابات الإجمالي.
- Feature: إضافة صفحة `تأكيد الاوردرات` بمسار تشغيل سريع يعتمد على الباركود وربط مباشر بالمندوب.
	- دعم إسناد الأوردرات المسموح بها فقط (`pending` و`returned`) مع منع التضارب بين المناديب.
	- دعم تجهيز الحالات مرحلياً إلى `مؤكدة` و`مؤجلة` و`ملغية` ثم حفظها دفعة واحدة.
	- تحسين الرسائل العربية وحالات المنع عند وجود الأوردر `مع المندوب` أو داخل يومية المندوب.
	- إضافة أزرار `حفظ` و`مسح` وتحسين تصميم بطاقات التنفيذ والملخصات.

## 1.1.5 (2026-02-14)
- Fix: Print layout improvements — larger company logo and barcode for clearer printouts.
- Fix: Corrected totals calculation to always use product line (price * quantity) when printing.
- Update: Print CSS adjusted so each printed order fills A4 page height and footer/policy aligns to bottom.
- Misc: Prepare release metadata and bumped package/version files to `1.1.5`.

## 1.1.4 (2026-02-14)
- Internal: previous quick fixes and sync (internal build alignment).

## 1.0.7 (2026-02-12)
- Hotfix: Prevent further api.php fatals by adding missing helpers used by orders import/journal/transfer flows.
- Release prep (2026-02-13): include DB reconciliation migrations and validation.
	- Migrations: `migrations/20260213_add_missing_tables_from_schema.sql` and `migrations/run_updates.php`.
	- Action: run the CREATEs first, then run `php migrations/run_updates.php` to apply conditional ALTERs on MySQL 5.7.

## 1.0.6 (2026-02-12)
- Hotfix: Fix fatal error on importing/saving orders due to missing `normalize_tax_discount_type()` in api.php.

## 1.0.5 (2026-02-12)
- Hotfix: Fix fatal error on saving/importing orders due to missing `get_setting_value()` in api.php.

## 1.0.4 (2026-02-11)
- Representatives insurance deposit system ("تأمين المناديب"):
	- Adds insurance fields to reps/users and prevents deleting reps when insurance exists.
	- Fixed dedicated treasury for insurance ("تأمين المناديب") with auto-creation and backward-compat safeguards.
	- Hide/disable insurance-related UI/treasury when reps are disabled via delivery method settings.
	- Locks insurance fields from editing when modifying an existing representative.
- Representative finance ("ماليات المندوب") improvements:
	- Adds settlement flow with corrected accounting: cash moves only through the selected treasury.
	- Net settlement is calculated as balance + insurance, then insurance is cleared via an internal adjustment and insurance fields are zeroed.
	- Adds internal transaction subtypes for rep penalty and insurance apply that do not require a treasury entry.
	- Hardens validation and error messages (e.g., insufficient treasury balance handling).
- New "أداء المناديب" page under Representatives menu:
	- Date presets (day/week/month/year) + specific-date mode.
	- Filter by a specific rep or all reps.
	- Top 10 representatives ranking.
	- Removes the old "أداء المندوب" box from the representative finance page.
- Backend reliability fixes:
	- Safer DB transaction handling (begin/commit/rollback guards).
	- Compatibility/self-healing schema checks for older installs to avoid missing-column failures.

## 1.0.3 (2026-02-09)
- POS as a standalone page that records real sales (stock + customer balance + treasury).
- Cash-customer flow with auto-paid behavior and improved receipt details (customer/paid/remaining).
- Delivery method setting (reps/direct/shipping) with UI gating and routing.
- Shipping Companies module and shipping-mode parity across sales screens.

## 1.0.2 (2026-02-09)
- Add in-app technical support section (RustDesk quick actions) in Settings.

## 1.0.0 (2026-02-09)
- Initial public release.
- Auto-update via GitHub Releases (server-side apply).
- Installer and schema reliability improvements.
- Permissions and representative access rules.
- Subfolder-safe asset paths and improved dev/prod API base-path handling.
